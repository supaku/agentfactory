/**
 * Shared test helpers for prompt tests.
 *
 * Exported only for test files — not part of the public API.
 */

import type { ArchScope } from '../types.js'

export const PROJECT_SCOPE_FOR_TEST: ArchScope = {
  level: 'project',
  projectId: 'agentfactory',
}
