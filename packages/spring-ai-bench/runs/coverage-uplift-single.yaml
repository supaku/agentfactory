# Run spec: Single-agent mode for baseline comparison
schema: bench.run.v1

case: coverage-uplift
runId: auto
outDir: bench-reports

agent:
  type: exec
  alias: agentfactory-single
  args: []

inputs:
  target-coverage: 80

environment:
  workingDir: "."
